# Demo Client Configuration

# Your Alchemy API Key (same as facilitator)
ALCHEMY_API_KEY=your-alchemy-api-key

# Private key for your test wallet (will create smart wallet from this)
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or use an existing testnet wallet
PRIVATE_KEY=your-private-key-here

# Facilitator URL (local or deployed)
FACILITATOR_URL=http://localhost:3000

# Test recipient address (where to send USDC payment)
# Default: Use facilitator operator's address or any test address
RECIPIENT_ADDRESS=0xd4323fC00E85E8353c7662808BBc3075f0D5BAa9

# Network to test on
NETWORK=base-sepolia

# Amount to send (in USDC, e.g., 0.01)
AMOUNT=0.01
